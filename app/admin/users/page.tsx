export const metadata = { title: 'Users — Admin — RuhMate' };

export default function AdminUsersPage() {
  return (
    <div className="rounded-card border border-line bg-white p-10">
      <h1 className="font-display text-3xl text-ink">Users</h1>
      <p className="mt-2 text-sm text-ink-muted">
        Admin user management lands in Phase 1 Day 5. The admin shell + auth gating are live.
      </p>
    </div>
  );
}
