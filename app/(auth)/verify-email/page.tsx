import { VerifyEmailPanel } from '@/components/auth/VerifyEmailPanel';

export const metadata = { title: 'Verify email — RuhMate' };

export default function VerifyEmailPage() {
  return <VerifyEmailPanel />;
}
