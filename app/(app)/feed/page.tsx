export const metadata = { title: 'Feed — RuhMate' };

export default function FeedPage() {
  return (
    <div className="rounded-card border border-line bg-white p-10 text-center">
      <h1 className="font-display text-3xl text-ink">Feed coming in Phase 2</h1>
      <p className="mt-2 text-sm text-ink-muted">
        Auth is live. Profile creation, AI-ranked feed, and contact reveal arrive next.
      </p>
    </div>
  );
}
